/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["portfolio.rajondey.com", "randomuser.me", "ui-avatars.com"], // Add more domains if needed
  },
  reactStrictMode: true,
};

module.exports = nextConfig;
