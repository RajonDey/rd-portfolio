# 1.0 OG artwork

## Status

`done`

## Goal

Link previews (Open Graph / Twitter) show a paper/ink document card that matches the v2 site: Senior Software Engineer, IBM Plex pairing, no tutorial icons, no Module Lead.

## Why

`0.10` kept `public/og-image.png`. That file is still the v1 navy graphic (code icons, “Software Developer & Module Lead”). Shares now contradict the live site. Listed as a possible `1.x` in `context/progress-tracker.md`. Owner asked to ship this; Academic CV stays a later plan.

## In scope

- Replace the v1 OG graphic with a 1200×630 document card generated from site identity strings.
- `src/app/opengraph-image.tsx` (and `twitter-image.tsx`) using `next/og` `ImageResponse`, IBM Plex files, `0.8` paper/ink tokens.
- Keep a static `public/og-image.png` snapshot of the same art so leftover `/og-image.png` URLs are not the navy card.
- Point share metadata at the generated image so crawlers pick the new file, not the stale v1 cache forever.

## Out of scope

- Academic CV.
- Per-route OG (work articles, writing). One site card.
- Dark mode, new brand colors, photos, code icons, years-of-experience badge.
- Changing titles, descriptions, JSON-LD, sitemap, or nav.

## User-facing behavior

1. Share `https://portfolio.rajondey.com` (or any page). Preview is a light paper card, not navy.
2. Card reads: kicker `FULL-STACK · LLM SECURITY`, name `Rajon Dey`, role `Senior Software Engineer`.
3. No “Module Lead”, no “Software Developer”, no laptop/`</>` icons, no teal on navy.
4. Twitter `summary_large_image` still applies.

## Content / copy

Reuse `src/lib/site.ts` only:

- `SITE_KICKER`
- `SITE_NAME`
- `SITE_ROLE`
- Host from `SITE_ORIGIN` (display `portfolio.rajondey.com`)
- Alt / `og:image:alt`: `SITE_TITLE`

Pills on the card: **Work** (filled) and **Writing** (outline), same labels as home CTAs. RD mark as in the nav (ink square, white “RD”).

## Files likely involved

- `src/app/opengraph-image.tsx`
- `src/app/twitter-image.tsx`
- `src/lib/site.ts` (`getPageShareMetadata` image URL)
- `public/og-image.png`
- IBM Plex TTF files used only by the OG renderer
- `context/progress-tracker.md`, `context/architecture.md`, `context/ui-context.md`, `specs/README.md`

## UI notes

Match `context/ui-context.md`. This is a 1200×630 still, not a page.

- Layout: paper field `#F4F1EA`, hairline frame, left-weighted type like the home index. RD mark top-left, host top-right. Kicker (mono) → name (serif) → role (sans). Hairline. Two pills.
- Tokens: paper `#F4F1EA`, ink `#1C1917`, muted `#57534E`. No forest green, teal, or gradient.
- Type: IBM Plex Serif Bold (name), Sans Semibold (role, pills, RD), Mono (kicker).
- Breakpoints: none. Fixed 1200×630.

## Acceptance criteria

- [x] Preview image is 1200×630, paper/ink, IBM Plex.
- [x] Copy matches `SITE_KICKER` / `SITE_NAME` / `SITE_ROLE`. No Module Lead.
- [x] No v1 icons or navy field.
- [x] `public/og-image.png` is the new card, not the navy original.
- [x] `npm run typecheck` passes
- [x] `/opengraph-image` (or equivalent) checked in the browser

## Open questions

None. Academic CV is a separate later spec.

## Notes for the agent

- Owner said proceed and to treat this creatively within v2 DNA (document, not illustration).
- Do not invent a second job title or years badge on the card.
- Do not add `@vercel/og` as a separate dependency; `next/og` is enough.
- Implemented: `src/app/opengraph-image.tsx` + `twitter-image.tsx`, IBM Plex in `src/app/og-fonts/`, snapshot written to `public/og-image.png`. Share metadata no longer hardcodes `/og-image.png`; Next injects the generated files.
- After implementation: status `done`, update the tracker.
