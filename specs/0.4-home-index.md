# 0.4 Home index

## Status

`done`

## Goal

`/` is a short hiring document: kicker, name, Senior Software Engineer, one sentence, dual CTAs (Work filled, Writing outline), three selected proofs from `HOME_EVIDENCE`, current role, and quiet links. No full-viewport hero, Earth, timeline, tech icons, expertise cards, testimonial teaser, or award gallery on this page.

## Why

A hiring manager should know title, stack, and one proof on the first screen. The live home is still the JS Mastery stack (hero animation, cards, timeline, Earth). `context/portfolio-v2.md` named Gourob’s *structure* (kicker, one sentence, selected evidence, inverted primary). Visual tokens wait for `0.8`. Depends on `0.1`–`0.3`.

## In scope

- Replace the `/` section stack with a document layout.
- Use locked copy below. Evidence rows come from `HOME_EVIDENCE` / helpers in `src/lib/selected-work.ts`. Do not pick a new trio.
- Dual CTAs: Work (filled, `/work`) and Writing (outline, `/writing`).
- Unmount tutorial sections from `src/app/page.tsx` only. Do not delete `EarthCanvas`, timeline, TechStack, Header animation, testimonials, or achievements files (`0.9`).
- `generateMetadata` on `/` is optional; root layout already has `SITE_TITLE`. Prefer not relying on `SEO.tsx`.
- Server Component. No Framer Motion on this page. No DeveloperAnimation.

## Out of scope

- `0.8` visual system (new fonts, cream paper, green accent, Gourob pixels).
- Academic CV button (no URL).
- Frontend Google Doc (About / `0.7`).
- Rewriting `/work/[slug]` articles (`0.5`).
- Expanding `/writing` (`0.6`) or `/about` (`0.7`).
- Deleting unused components or `/projects` / `/testimonials` / `/achievements` routes (`0.9`).
- Changing the sticky nav (already Work · Writing · About · CV).
- WhatsApp as a home CTA (leave the open question for `0.7` / `0.9`). Email, GitHub, LinkedIn only on this page.
- Relocation sentence.

## User-facing behavior

1. Open `/`. First screen is a document, not a split hero with animation. No Earth. No timeline. No tech-icon tabs. No expertise cards. No testimonial or achievements blocks.
2. Kicker reads `FULL-STACK · LLM SECURITY`. H1 is `Rajon Dey`. Role line is `Senior Software Engineer`. No Module Lead on those lines. No “Hi, I’m”.
3. One supporting paragraph (below). Then two controls: **Work** (filled, goes to `/work`) and **Writing** (outline, goes to `/writing`).
4. Three evidence rows, in `HOME_EVIDENCE` order: CalystaPro EMR, IELTS platform, IEEE paper. Work rows go to `/work/{slug}`. IEEE opens the Xplore URL in a new tab.
5. A short current-role line uses the real SJI title from `src/lib/data.ts` (`Module Lead (Frontend)`) plus `getCurrentRoleDateRange()`. That is employment fact, not the H1.
6. Connect: email, GitHub, LinkedIn as text links. CV stays in the nav (same PDF). No Academic CV. No WhatsApp button.
7. Footer unchanged from `0.1`.
8. `/work`, `/writing`, `/about` still work. Old `/projects` still works if typed.

## Content / copy

Voice: short, plain, no em dashes, no “passionate” / “innovative solutions”. Reuse `SITE_NAME`, `SITE_ROLE`, `CV_URL`, `GITHUB_URL`, `CONTACT_EMAIL`, `IEEE_*` from `src/lib/site.ts`.

| Slot | Copy |
| ---- | ---- |
| Kicker | `FULL-STACK · LLM SECURITY` |
| H1 | `Rajon Dey` |
| Role | `Senior Software Engineer` |
| Supporting sentence | `Full-stack. React, Next.js, TypeScript, Node.js, Python. {yearsLabel} in healthcare and SaaS. Also AI and LLM security research (IEEE).` |
| Primary CTA | `Work` → `/work` |
| Secondary CTA | `Writing` → `/writing` |
| Evidence heading | `Selected` |
| Current role | `{experiences[0].title}, {experiences[0].company}. {getCurrentRoleDateRange()}.` |
| Connect heading | `Connect` |
| Connect links | `Email` (`mailto:contact@rajondey.com`), `GitHub` (`GITHUB_URL`), `LinkedIn` (`https://linkedin.com/in/rajondey`) |

`{yearsLabel}` is `getYearsOfExperienceLabel()`.

Evidence rows: existing `title` + `description` (case study / project detail / IELTS project description). IEEE row title is `IEEE_PAPER_TITLE`. IEEE one-liner: `IEEE ICCIT 2025`. Do not invent metrics.

Do not put a filled Publications pill. Do not add a second H1 (“and researcher”).

## Files likely involved

- `src/app/page.tsx`
- `src/lib/selected-work.ts` (`getHomeEvidenceItems` or equivalent)
- `src/lib/site.ts` (optional `LINKEDIN_URL`, kicker constant)
- `src/lib/data.ts` (read current role only)
- `src/lib/experience.ts` (`getCurrentRoleDateRange`, years label)
- `src/components/Footer.tsx` (keep)

Do not edit `Header.tsx` except if it is unused after unmount. Prefer leaving the file for `0.9`.

## UI notes

- Layout: **document**, left-aligned, similar padding to `/about` and `/work` (`py-20`, `max-w-7xl mx-auto px-4 sm:px-6 lg:px-8`). Not `min-h-screen` centered hero. Not two-column animation.
- Rhythm: kicker → name → role → one paragraph → two buttons → selected list → current role → connect. Steal Gourob’s *order*, not his forest green or four pills.
- Tokens: existing `text-textDark`, `text-textLight`, `primary`, `accent`, `rounded-lg`. Filled Work button uses the same classes as inner-page `CV` (`bg-primary text-white … hover:bg-accent`). Writing uses the outline Email button (`border border-primary text-primary …`).
- Breakpoints: stack as now (`flex-col sm:flex-row` for the two CTAs). Evidence is a text list, not a card grid.
- Motion: none on this page.
- Nav chrome: unchanged.

## Acceptance criteria

- [x] `/` H1 is Rajon Dey. Role is Senior Software Engineer. Kicker is `FULL-STACK · LLM SECURITY`.
- [x] Supporting sentence matches the table (years still computed).
- [x] Work (filled) → `/work`. Writing (outline) → `/writing`. No Academic CV. No WhatsApp on `/`.
- [x] Three evidence items: CalystaPro, IELTS, IEEE, in that order, correct hrefs.
- [x] Current role line still says Module Lead (Frontend) at SJ Innovation. H1 does not.
- [x] No Earth, DeveloperAnimation, TechStack, Experience timeline, home project cards, Achievements, or Testimonials on `/`.
- [x] Those components still exist on disk. `/projects`, `/testimonials`, `/achievements` still 200.
- [x] Page is a Server Component. No new visual tokens.
- [ ] `npm run typecheck` passes except pre-existing `/showcase` WIP.
- [x] Browser-check `/` plus click through Work, Writing, one evidence row, IEEE (external).

`npm run lint` is clean. `/` verified via rendered HTML.

## Open questions

None for this spec. WhatsApp, relocation, and About quotes stay deferred.

## Notes for the agent

- Implemented in the same pass after the owner said to proceed with the next unit.
