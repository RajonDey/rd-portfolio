# 0.6 Writing (IEEE)

## Status

`done`

## Goal

`/writing` is the research / scholarship lane. The IEEE ICCIT 2025 paper is the **only** post. No invented blog. Rajon is not titled researcher on this page.

## Why

Scholarship and AI-adjacent readers need one click to the paper. The `0.2` shell already lists IEEE. This unit locks the page as a document, moves copy into `src/lib/`, and attaches only URLs that already exist in the repo. Depends on `0.1`–`0.2`. Listed next in `context/progress-tracker.md`.

## In scope

- `/writing` as a document list with IEEE as the lead (and only) item.
- Put writing records in `src/lib/writing.ts` (or `site.ts`). Page reads data, does not hardcode the paper title.
- One-line summary from locked `0.1` copy: comment-based attacks against LLMs.
- Optional extra links on **that same item** only if they already exist on `projectDetails` slug `llm-security-research-platform` (research site, Zenodo, GitHub). Not separate posts.
- Keep `generateMetadata` title `Writing · Rajon Dey`.
- Server Component. Footer. `ContactCta` may stay.

## Out of scope

- Invented blog posts, Medium index, or Academic CV.
- Dual H1 “engineer and researcher”.
- Putting IEEE on `/work`.
- Home, `/work` articles, `/about`.
- Visual system (`0.8`).
- `/achievements` (IEEE must not move back into a certificate gallery).

## User-facing behavior

1. Open `/writing`. H1 is `Writing`. Role is not on this page as a second career.
2. Intro line: `IEEE research on LLM security. This is not a blog.`
3. One item: full IEEE paper title, venue `IEEE ICCIT 2025`, one summary sentence, primary link to IEEE Xplore (new tab).
4. Same item may show extra links (Research site, Dataset, GitHub) from existing `portfolio.ts` URLs. No second row.
5. No other posts.

## Content / copy

| Slot | Copy |
| ---- | ---- |
| H1 | `Writing` |
| Intro | `IEEE research on LLM security. This is not a blog.` |
| Item title | `IEEE_PAPER_TITLE` from `src/lib/site.ts` |
| Venue | `IEEE ICCIT 2025` |
| Summary | `A published IEEE paper on comment-based attacks against LLMs.` (from `0.1` intro; do not write a new abstract) |
| Primary href | `IEEE_URL` (`https://ieeexplore.ieee.org/document/11491067`) |
| Extra links (optional, same item) | From `llm-security-research-platform` in `portfolio.ts`: Research Site, Dataset (Zenodo), GitHub. Use those labels and URLs as stored. |

Document title / description: keep `Writing · Rajon Dey` and `IEEE research on LLM security. Writing by Rajon Dey.`

## Files likely involved

- `src/app/writing/page.tsx`
- `src/lib/writing.ts` (new) and/or `src/lib/site.ts`
- `src/lib/portfolio.ts` (read extra links only)

## UI notes

- Layout: document, same padding as `/work` and `/about` (`py-20`, `max-w-7xl` or `max-w-3xl`). Text list, not cards.
- Tokens: `text-textDark`, `text-textLight`, `primary`. Title is a text link like `/work` rows.
- Extra links: simple text links under the summary, not a button row of new styles. Existing hover `hover:text-primary` is enough.
- Breakpoints: stack.
- Motion: none.

## Acceptance criteria

- [x] `/writing` H1 is Writing. One paper. No researcher H1.
- [x] IEEE Xplore URL is the primary link. Title is the locked full title.
- [x] No posts that are not in this spec.
- [x] Extra links, if shown, match `portfolio.ts` for `llm-security-research-platform`.
- [x] Copy lives in `src/lib/`, not only in JSX.
- [x] `npm run typecheck` passes except pre-existing `/showcase` WIP.
- [x] Browser-check `/writing` and the IEEE link.

## Open questions

- Any real posts besides IEEE? **No** in the career brief or `src/lib`. Do not add Medium.

## Notes for the agent

- Owner said to proceed with the next unit. Implement after filling this spec.
- Do not scrape or invent abstracts.
- After implementation: status `done`, update `context/progress-tracker.md`.
