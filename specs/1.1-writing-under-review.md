# 1.1 Writing under review

## Status

`done`

## Goal

`/writing` still leads with the published IEEE paper. A second row names the under-review Elsevier survey. One quiet line links Developer Data. No post archive, no home change, no new JSON-LD.

## Why

Owner asked to add a real manuscript under review and the [Developer Data](https://developer-data.beehiiv.com/) newsletter without turning Writing into a blog. Agreed in chat: survey on `/writing` below IEEE; newsletter as one index link; IEEE stays the home proof. Follow-up to `0.6`. Academic CV remains a later spec.

## In scope

- Second writing record in `src/lib/writing.ts` (under review, no public URL).
- Optional `href` on writing items so the survey title is text, not a fake link.
- Intro copy that still says this is not a blog.
- One sentence under the list: I also write at Developer Data (external index URL).
- Metadata description aligned with the new intro.

## Out of scope

- Home `HOME_EVIDENCE`. Nav. About. Footer.
- JSON-LD ScholarlyArticle for the survey.
- Listing individual newsletter posts.
- Author list, DOI, PDF, or a published claim.
- Academic CV.
- Visual restyle.

## User-facing behavior

1. Open `/writing`. H1 is Writing. Intro mentions IEEE and one manuscript under review, and that this is not a blog.
2. First row is the IEEE paper (title links to Xplore, extra links unchanged).
3. Second row is the survey title as plain text, venue/status line, contribution summary. No title href.
4. Below the list, one line: I also write at Developer Data. Developer Data opens `https://developer-data.beehiiv.com/` in a new tab.
5. Home still shows IEEE as the only writing proof.

## Content / copy

No em dashes.

| Slot | Copy |
| ---- | ---- |
| H1 | `Writing` (unchanged) |
| Intro | `IEEE research on LLM security, and one manuscript under review. This is not a blog.` |
| Item 1 | Existing IEEE record (`0.6`). Unchanged. |
| Item 2 title | `Agentic Artificial Intelligence: A Survey of Architectures, Evolutionary Dynamics, and Governance` |
| Item 2 venue | `Under review, 2026. Information Processing & Management (Elsevier).` |
| Item 2 summary | `Survey of agentic architectures, how those systems evolve in practice, and the governance questions that follow from deploying them. Co-authored with EARL collaborators.` |
| Item 2 href | none |
| Newsletter | Link text `Developer Data`. URL `https://developer-data.beehiiv.com/`. Sentence: `I also write at Developer Data.` |
| Document description | `IEEE research on LLM security, and one manuscript under review. Writing by Rajon Dey.` |

Venue uses the journal’s public name, including the ampersand. Do not shorten to IP&M on the page.

## Files likely involved

- `src/lib/writing.ts`
- `src/app/writing/page.tsx`
- `context/architecture.md`
- `context/project-overview.md`
- `context/progress-tracker.md`
- `specs/README.md`

## UI notes

- Layout: same document list as `0.6`. Survey row uses the same type scale; title without `href` is not a link (no underline).
- Newsletter sentence: `text-textLight` under the list, `max-w-3xl`, `mt-12`. Name is a `text-textDark` underline-on-hover link like other text links.
- Tokens / components: existing `/writing` only. `ContactCta` stays.
- Breakpoints: stack. Motion: none.

## Acceptance criteria

- [x] `/writing` lists IEEE first, survey second. Survey title is not a link.
- [x] Copy matches the table. Status is under review, not published.
- [x] Developer Data is one index link. No beehiiv post list.
- [x] Home evidence and SchemaOrg ScholarlyArticle unchanged (IEEE only).
- [x] `npm run typecheck` passes
- [x] Touched routes checked in the browser (`/writing`; home still has IEEE only)

## Open questions

None. Owner confirmed this split.

## Notes for the agent

- Do not invent co-author names, a preprint URL, or metrics.
- If the survey is later accepted or rejected, that is a new spec (add official URL, or remove the row).
- Owner confirmed this split. Implemented.
