# 0.7 About

## Status

`done`

## Goal

`/about` is the facts page: identity copy, experience as a text list, stack as a sentence, one location line, default CV plus frontend CV. Not a timeline, not a testimonial wall, not a second career brand.

## Why

Hiring managers who open About need employment fact, stack, and where Rajon is / will work. The `0.2` shell is only the two `0.1` paragraphs plus CV and Email. `context/career-brief.md` and `context/portfolio-v2.md` already named this page. Depends on `0.1`–`0.2`. Listed next in `context/progress-tracker.md`.

## In scope

- Rebuild `/about` as a document: keep the two `0.1` intro paragraphs; add experience list, stack sentence, location line, CV variants, quiet connect links.
- Read roles from existing `experiences` in `src/lib/data.ts`. Do not rewrite bullets. Do not change `Module Lead (Frontend)`.
- Put About-only strings (stack sentence, location, frontend CV URL) in `src/lib/` (`site.ts` and/or a small `about.ts`). Page does not hardcode URLs.
- Keep `generateMetadata` title `About · Rajon Dey` and `getSiteDescription()`.
- Server Component. Footer. Existing tokens.

## Out of scope

- Changing the SJI job title `Module Lead (Frontend)` in `src/lib/data.ts` (employment fact).
- Academic CV (no URL). Master CV Google Doc (internal).
- Invented roles, education block, salary, notice period, or visa-product copy (Blue Card, Kennismigrant, Express Entry).
- Testimonials on this page. Do not invent quotes. Do not cherry-pick from `testimonials.ts`.
- WhatsApp.
- Mounting `TechStack`, `Experience` timeline, or `ContactCta` (CV/Email would duplicate).
- Home, `/work`, `/writing`.
- Visual system (`0.8`).
- Retiring `/testimonials` (`0.9`).

## User-facing behavior

1. Open `/about`. H1 is `About`. Role line is `Senior Software Engineer`. No researcher H1. No Module Lead in the H1 or role line.
2. Two intro paragraphs from `0.1` (years still computed). IEEE stays a secondary clause, not a second career.
3. **Experience** is a text list from `experiences` (SJI first, then freelance). Each role: title, company, date, then the existing `description[]` as a plain list. No 3D timeline. No icons.
4. **Stack** is one sentence (below). No icon grid. Do not dump `techStack[]` (no Vue/PHP/Jest identity dump).
5. **Location** is one sentence (below). Do not lead with visa strategy. Do not list Japan or excluded countries.
6. Links: **CV** (filled, same PDF as nav), **Frontend CV** (Google Doc, new tab), **Email**, **GitHub**, **LinkedIn**. No WhatsApp. No Academic CV.
7. `/testimonials` still 200 if typed. It is not linked from About.

## Content / copy

Voice: short, plain, no em dashes. Reuse `SITE_ROLE`, `CV_URL`, `CONTACT_EMAIL`, `GITHUB_URL`, `LINKEDIN_URL`, `getYearsOfExperienceLabel()`.

| Slot | Copy |
| ---- | ---- |
| H1 | `About` |
| Role | `Senior Software Engineer` |
| Intro p1 | `I am a Senior Software Engineer at SJ Innovation. I build production web applications with React, Next.js, TypeScript, Node.js, and Python. I own frontend architecture and work on APIs, auth, and integrations.` |
| Intro p2 | `Work includes healthcare and SaaS platforms and CI/CD with GitHub Actions. Secondary: a published IEEE paper on comment-based attacks against LLMs, for AI security and research-track readers. {yearsLabel} of corporate experience since 2019.` |
| Experience heading | `Experience` |
| Stack heading | `Stack` |
| Stack sentence | `React, Next.js, TypeScript, Node.js, Python, FastAPI. AWS, Docker, and CI/CD.` |
| Location heading | `Location` |
| Location sentence | `Based in Sylhet, Bangladesh. Open to Germany, Netherlands, and Canada.` |
| CV label | `CV` |
| CV href | `CV_URL` (`https://www.rajondey.com/cv.pdf`) |
| Frontend CV label | `Frontend CV` |
| Frontend CV href | `https://docs.google.com/document/d/1FTe6VOEeQ-6YLV0rboZaTrOGnCkynpp_3k8BKYNKu7M` |
| Email | `mailto:{CONTACT_EMAIL}` |
| GitHub | `GITHUB_URL` |
| LinkedIn | `LINKEDIN_URL` |

`{yearsLabel}` is `getYearsOfExperienceLabel()`.

Experience rows: do not invent. Render `src/lib/data.ts` `experiences` as-is, including the freelance 2015–2018 role. The `~30%` load-time line stays only because it is already in that array; do not add it to the intro.

Document title / description: keep `About · Rajon Dey` and `getSiteDescription()`.

## Files likely involved

- `src/app/about/page.tsx`
- `src/lib/site.ts` (add `FRONTEND_CV_URL`; optional location/stack constants)
- `src/lib/about.ts` (optional; intro + stack + location if not in `site.ts`)
- `src/lib/data.ts` (read `experiences` only)
- `src/lib/experience.ts` (years label)
- `src/components/Footer.tsx` (keep)

## UI notes

- Layout: document, same padding as `/work` and `/writing` (`py-20`, `max-w-7xl mx-auto px-4 sm:px-6 lg:px-8`). Body column `max-w-3xl`. Text list, not cards, not the old timeline.
- Tokens: `text-textDark`, `text-textLight`, `primary`, `accent`. CV filled button matches inner-page CV (`bg-primary text-white … hover:bg-accent`). Frontend CV is a text link (`hover:text-primary`), not a second filled button. Email / GitHub / LinkedIn are text links like home Connect.
- Section headings: `text-2xl` or `text-3xl font-bold text-textDark`, same family as Work/Writing. Experience items: title bold, company and date `text-textLight`.
- Breakpoints: stack. CV button may sit `flex-col sm:flex-row` with the text links below.
- Motion: none. No Framer. No Earth.

## Acceptance criteria

- [x] `/about` H1 is About. Role is Senior Software Engineer. No researcher H1.
- [x] Two `0.1` intro paragraphs. Years still computed.
- [x] Experience list shows both existing roles. SJI title remains `Module Lead (Frontend)`.
- [x] Stack is the one sentence above. No `TechStack` icons.
- [x] Location sentence matches the table. No visa-product wording.
- [x] CV opens the PDF. Frontend CV opens the Google Doc. No Academic CV. No WhatsApp. No quotes.
- [x] Copy/URLs live in `src/lib/`, not only in JSX.
- [x] Home, `/work`, `/writing` unchanged. `/testimonials` still 200.
- [x] `npm run typecheck` passes except pre-existing `/showcase` WIP.
- [x] Browser-check `/about` and both CV links.

## Open questions

None left for implementation. Decisions locked in this spec:

| Question | Decision |
| -------- | -------- |
| Relocation sentence | **Yes.** One factual line. No visa products. |
| WhatsApp | **Drop** on About. Email / GitHub / LinkedIn only. Remaining WhatsApp UI is `0.9`. |
| Testimonials on About | **None.** Existing quotes stay on `/testimonials` until `0.9`. Do not invent replacements. |

Change a row before marking `ready` if you disagree.

## Notes for the agent

- Owner said proceed. Implement against this spec.
- Do not invent an academic CV or extra jobs.
- Do not edit `testimonials.ts` or experience bullets.
- Default public CV stays the PDF. Frontend CV is the Google Doc in the table, not the SWE or master Docs.
- After implementation: status `done`, update `context/progress-tracker.md`.
