# 0.10 SEO and metadata

## Status

`done`

## Goal

Document head, Open Graph, Twitter, JSON-LD, PWA manifest, robots, and sitemap match the public identity (Senior Software Engineer; IEEE as secondary) and the live v2 routes. No tutorial leftover in crawlable metadata.

## Why

`0.1` locked identity strings. Routes and footer archives now exist (`0.2`, `0.11`). Manifest still points at `/#projects`, `/#experience`, `/#contact` and lists Three.js / Wix / Squarespace. JSON-LD still dumps every social URL. Listed in `context/progress-tracker.md`.

## In scope

- Root `metadata` in `src/app/layout.tsx`: keep `0.1` title/description; add `title.template` only if it does not double “Rajon Dey”; set `alternates.canonical` for `/`.
- Per-route `metadata` / `generateMetadata` already on inner pages: keep titles below; add `openGraph.url` where missing so shares pick the page, not only the home OG block.
- JSON-LD Person: `jobTitle` stays Senior Software Engineer. `sameAs` is GitHub + LinkedIn only. Keep occupation skills from `0.1`. Add a second JSON-LD object for the IEEE paper as `ScholarlyArticle` (name + url from `site.ts`). Not a second job title.
- `public/manifest.json`: identity from `0.1`; drop Three.js / Wix / Squarespace / Shopify / WordPress as identity categories; drop hash shortcuts; shortcuts Work `/work`, Writing `/writing`, About `/about` if shortcuts stay. Theme/background hex from `0.8` (`#1C1917`, `#F4F1EA`).
- `public/sitemap.xml`: `/`, `/work`, `/writing`, `/about`, footer `/testimonials` and `/achievements`, plus the six `SELECTED_WORK_SLUGS` as `/work/{slug}`. Do not list off-list work slugs, `/projects`, or `/showcase`.
- `public/robots.txt`: keep Allow `/` and the sitemap line.
- Do not restore `SEO.tsx`.

## Out of scope

- Changing `0.1` identity sentences, H1 copy, or CV URLs.
- Invented keywords, blog posts, or a Medium index.
- New OG artwork (keep `/og-image.png`).
- Visual restyle (`0.8`). New pages.
- `next.config` `images.domains` deprecation (not metadata).

## User-facing behavior

1. View source on `/`. `<title>` is `Rajon Dey · Senior Software Engineer`. Meta description is `getSiteDescription()`. OG `og:site_name` is `Rajon Dey`. No Module Lead in title or JSON-LD `jobTitle`.
2. `/work`, `/writing`, `/about`, `/testimonials`, `/achievements` keep the titles in the table. `/work/[slug]` title is `{record.title} · Rajon Dey` and description is the existing description/overview.
3. JSON-LD Person `sameAs` is only GitHub and LinkedIn. A ScholarlyArticle node names the IEEE paper and `IEEE_URL`.
4. PWA manifest has no `/#projects` shortcut and no Three.js / Wix categories.
5. `sitemap.xml` lists the v2 URLs above. `robots.txt` still allows crawl.

## Content / copy

Reuse `0.1`. `{yearsLabel}` is `getYearsOfExperienceLabel()`.

| Surface | Title | Description |
| ------- | ----- | ----------- |
| `/` (default) | `Rajon Dey · Senior Software Engineer` | `getSiteDescription()` |
| `/work` | `Work · Rajon Dey` | `Selected work. Healthcare, SaaS, and production web applications.` |
| `/work/[slug]` | `{title} · Rajon Dey` | Existing record description / overview |
| `/writing` | `Writing · Rajon Dey` | `IEEE research on LLM security. Writing by Rajon Dey.` |
| `/about` | `About · Rajon Dey` | `getSiteDescription()` |
| `/testimonials` | `Testimonials · Rajon Dey` | `Notes from colleagues. Rajon Dey.` |
| `/achievements` | `Achievements · Rajon Dey` | `Awards and certificates. Rajon Dey.` |

OG `site_name`: `Rajon Dey`. OG image: `/og-image.png` (1200×630), alt = `SITE_TITLE`. Twitter card: `summary_large_image`.

JSON-LD Person:

- `name`: `SITE_NAME`
- `jobTitle` / occupation `name`: `SITE_ROLE`
- `description`: `getSiteDescription()`
- `url`: `https://portfolio.rajondey.com`
- `email`: `CONTACT_EMAIL`
- `sameAs`: `GITHUB_URL`, `LINKEDIN_URL` (https form, consistent)
- occupation `description` and `skills`: unchanged from `0.1`
- `address`: Sylhet, Bangladesh (already there)

JSON-LD ScholarlyArticle (sibling in an `@graph` or a second script):

- `name`: `IEEE_PAPER_TITLE`
- `url`: `IEEE_URL`
- `author`: Person `SITE_NAME`

Manifest `description`: static `SITE_DESCRIPTION_STATIC` (no years). Categories: `portfolio`, `software engineering` only (or omit categories). No Three.js, Wix, Squarespace, Shopify, WordPress.

## Files likely involved

- `src/app/layout.tsx`
- `src/app/SchemaOrg.tsx`
- `src/app/work/page.tsx`, `src/app/work/[slug]/page.tsx`
- `src/app/writing/page.tsx`, `src/app/about/page.tsx`
- `src/app/testimonials/page.tsx`, `src/app/achievements/page.tsx`
- `src/lib/site.ts` (reuse only)
- `src/lib/selected-work.ts` (sitemap slugs)
- `public/manifest.json`
- `public/sitemap.xml`
- `public/robots.txt`

## UI notes

- None. Head tags and JSON only. No layout or token changes.

## Acceptance criteria

- [x] Home title and description match `0.1`. No Module Lead in head or JSON-LD job title.
- [x] Inner page titles match the table. Work detail uses existing record copy.
- [x] JSON-LD `sameAs` is GitHub + LinkedIn. IEEE is a ScholarlyArticle, not a second H1.
- [x] Manifest has no hash shortcuts and no Wix/Three.js identity.
- [x] Sitemap lists v2 routes + six selected work URLs. No `/projects` or `/showcase`.
- [x] `SEO.tsx` stays gone.
- [x] `npm run typecheck` passes.
- [x] View-source check `/`, `/work`, `/writing`, one `/work/[slug]`.

## Open questions

None left for implementation. Decisions locked:

| Question | Decision |
| -------- | -------- |
| `SEO.tsx` | Already deleted in `0.9`. Do not restore. |
| JSON-LD social dump | GitHub + LinkedIn only. |
| Sitemap work URLs | Selected six only. |
| New OG image | No. Keep `/og-image.png`. |

Change a row before marking `ready` if you disagree.

## Notes for the agent

- Owner asked to write this spec, then said proceed. Implemented.
- Do not invent keywords or metrics.
- After implementation: status `done`, update `context/progress-tracker.md`.
