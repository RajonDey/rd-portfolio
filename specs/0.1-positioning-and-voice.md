# 0.1 Positioning and voice

## Status

`done`

## Goal

Every public identity string presents Rajon as a **Senior Software Engineer** (full-stack) first, with **AI / LLM security research as a secondary line** (IEEE paper; scholarships and AI-adjacent roles later). Not a dual H1 like “engineer and researcher”. Visible copy follows the locked voice rules. The default CV is the published industry PDF. Layout, routes, and visual system stay as they are.

## Why

Hiring for Germany / Netherlands / Canada senior roles is the primary job of the site. Research/scholarships are a real secondary path and must be named, not hidden in a certificate gallery, and not allowed to take the title. The live site leads with `Module Lead (Frontend) | Senior Software Engineer` and tutorial filler. This unit is the copy lock. Header chrome like Gourob’s kicker + four pills is **`0.4`**. Overall UI/UX is **`0.8`**, not this spec.

Listed as next up in `context/progress-tracker.md`.

## In scope

- Replace identity copy: hero, introduction, metadata, Open Graph, Twitter, JSON-LD, PWA manifest, repeated page titles, CTA band copy, CV button labels.
- Keep `getYearsOfExperienceLabel()` wherever years appear. Do not hardcode "6+ years".
- Default CV href stays the published PDF. Label it `CV`.
- Fix the hero GitHub URL to `https://github.com/RajonDey`.
- Trim JSON-LD `jobTitle`, description, occupation name, and skills so they match SWE positioning (drop Three.js / Wix / Squarespace as identity skills).
- Strip banned phrases from **site chrome copy** (hero, intro, CTA bands, metadata). Do not rewrite case-study bodies or testimonial quotes in this spec.
- Name the secondary research lane in hero supporting copy, intro, and meta description. Do not add a kicker row, pill row, or Academic CV button (those are `0.4`, and Academic CV needs a URL we do not have).

## Out of scope

- New routes (`/work`, `/writing`, `/about`) or nav IA (`0.2`).
- Choosing which three home proofs or which 4–6 work items (`0.3`).
- Rebuilding home as a document (`0.4`).
- Case study article rewrite (`0.5`).
- IEEE page (`0.6`).
- About page, frontend CV Google Doc, relocation sentence (`0.7`).
- Fonts, colors, cream paper, green accent, pill buttons, left-document hero (`0.8` UI/UX, and header *structure* in `0.4`).
- Removing Earth / timeline / tech icons / expertise cards (`0.9`).
- Cloning Gourob’s visual design.
- Sitemap / full SEO pass beyond the title and description strings below (`0.10`).
- Changing WhatsApp vs email as the contact control (leave the existing Contact / WhatsApp buttons).
- Editing `src/lib/testimonials.ts` quote text.
- Editing case study / project **body** copy in `src/lib/portfolio.ts`.
- Changing the experience **job title** in `src/lib/data.ts` (`Module Lead (Frontend)` at SJI is the real role and must stay on the timeline).

## User-facing behavior

1. Open `/`. Name is **Rajon Dey**. Role line is **Senior Software Engineer**. No "Hi, I'm". No Module Lead. No "Graduate Researcher" in the title.
2. Hero supporting sentence is full-stack + years, then one short clause on AI / LLM security research. CV button says **CV** and opens `https://www.rajondey.com/cv.pdf`. GitHub icon goes to `https://github.com/RajonDey`.
3. Introduction is two short paragraphs. Research is in paragraph 2 as IEEE, not as a second career. No "passionate", no "Ready to discuss how I can contribute", no shopping for "remote-first or relocation-ready roles" on this page.
4. View source / social preview: document title and description use the strings in Content below, not Module Lead in the H1 sense.
5. Inner pages that currently say "Interested in Working Together?" / "passionate" / "innovative solutions" use the replacement CTA copy below. Same buttons and layout.
6. JSON-LD Person `jobTitle` is `Senior Software Engineer`.
7. Experience section still shows **Module Lead (Frontend)** at SJ Innovation. That is employment fact, not homepage branding.

## Content / copy

Use these strings. No em dashes. No extra slogans.

### Identity (reuse everywhere it currently appears)

| Slot | Copy |
| ---- | ---- |
| Name | `Rajon Dey` |
| Role | `Senior Software Engineer` |
| Supporting (hero) | `Full-stack. React, Next.js, TypeScript, Node.js, Python. {yearsLabel} in healthcare and SaaS. Also AI and LLM security research (IEEE).` |
| Document title | `Rajon Dey · Senior Software Engineer` |
| Meta description | `Senior Software Engineer. Full-stack work with React, Next.js, TypeScript, Node.js, and Python. {yearsLabel} at SJ Innovation. Secondary: published IEEE research on LLM security.` |
| JSON-LD `jobTitle` | `Senior Software Engineer` |
| JSON-LD Person `description` | Same as meta description. |
| JSON-LD occupation `name` | `Senior Software Engineer` |
| JSON-LD occupation `description` | `Builds production web applications with React, Next.js, TypeScript, Node.js, and Python.` |
| Manifest `name` | `Rajon Dey · Senior Software Engineer` |
| Manifest `short_name` | `Rajon Dey` |
| Manifest `description` | Same as meta description (you may omit `{yearsLabel}` here if the JSON is static; then use `Senior Software Engineer. Full-stack work with React, Next.js, TypeScript, Node.js, and Python.`). |
| OG `site_name` | `Rajon Dey` |
| CV label | `CV` |
| CV href | `https://www.rajondey.com/cv.pdf` |

`{yearsLabel}` means `getYearsOfExperienceLabel()` (e.g. `7+ years`).

### Hero

- H1: `Rajon Dey`
- H2: `Senior Software Engineer`
- Paragraph: supporting sentence above
- Primary button: `CV` (same PDF href, `rel="noopener noreferrer"` if missing)
- Secondary button: leave label and WhatsApp href as they are
- GitHub: `https://github.com/RajonDey`
- LinkedIn: `https://linkedin.com/in/rajondey` (already correct)

Do not add the frontend Google Doc, master CV Doc, or an Academic CV control on this page.

### Locked strings for later specs (do not build here)

| Later spec | String / rule |
| ---------- | ------------- |
| `0.4` kicker | `FULL-STACK · LLM SECURITY` |
| `0.4` primary pill | Work (jobs). Filled style. Not Publications. |
| `0.4` secondary pill | Writing (IEEE / research / scholarships) |
| `0.4` industry CV | Same PDF as this spec |
| `0.4` academic CV | **Omit** until the owner gives a URL |
| `0.8` | Visual system. Do not start UI/UX before `0.4` exists. |

### Introduction

Replace the four paragraphs plus "Ready to discuss..." with:

**Heading:** keep `At a Glance.` (structure unchanged)

**Paragraph 1:** `I am a Senior Software Engineer at SJ Innovation. I build production web applications with React, Next.js, TypeScript, Node.js, and Python. I own frontend architecture and work on APIs, auth, and integrations.`

**Paragraph 2:** `Work includes healthcare and SaaS platforms and CI/CD with GitHub Actions. Secondary: a published IEEE paper on comment-based attacks against LLMs, for AI security and research-track readers. {yearsLabel} of corporate experience since 2019.`

Keep the existing intro CV button; change its label to `CV`. Keep the four expertise cards for now (`0.9` removes them). Do not retitle those cards in this spec.

The `~30% load-time` claim may stay **out** of this intro (it is not in the career brief). Do not invent a replacement metric.

### CTA bands (projects, case studies, achievements, testimonials, showcase)

Same two-button layout. Replace heading and body:

- Heading: `Contact`
- Body: `contact@rajondey.com`
- Buttons: `CV` (PDF) and `Email` (`mailto:contact@rajondey.com`)

Drop: `Interested in Working Together?`, `I'm passionate...`, `innovative solutions`, `bring your ideas to life`, `Download My CV`, `Get In Touch` as the primary label (use `Email`).

### Footer

- Keep copyright line.
- Second line: `Building since {getCareerStartLabel()}.`
- Remove `Made with ❤️ by Rajon Dey`.

### JSON-LD skills list

Replace the current skills array with only:

`React`, `Next.js`, `TypeScript`, `Node.js`, `Python`, `FastAPI`, `AWS`, `Docker`, `CI/CD`

Do not add Three.js, Wix, Squarespace, or Shopify as identity skills.

### Page `<SEO>` titles that still say Module Lead or "Software Developer"

Use `Rajon Dey · Senior Software Engineer` as the site suffix. Examples:

- Home: `Rajon Dey · Senior Software Engineer`
- Projects: `Work · Rajon Dey`
- Testimonials: `Testimonials · Rajon Dey`
- Achievements: `Achievements · Rajon Dey`
- Case study list (if still reachable): `Case studies · Rajon Dey`

Do not spend this spec rewriting every case-study H1. Detail pages that already use the project name can stay; only fix titles that still contain `Module Lead` or a stacked title.

### Banned on any string this spec touches

- `Hi, I'm` / `Hi, I’m`
- `Module Lead (Frontend) |` in hero, metadata, schema `jobTitle`
- `Graduate Researcher`, `AI Software Engineer and …` as the role line
- em dashes
- `passionate`, `innovative solutions`, `bring your ideas to life`, `impactful solutions`, `Ready to discuss how I can contribute`
- `Check Resume`, `Download My CV` (use `CV`)
- `full-stack solutions that drive real business impact`

### Must not change

- `src/lib/data.ts` experience title `Module Lead (Frontend)`
- Testimonial quote text
- Case study / project narrative fields in `portfolio.ts`

## Files likely involved

- `src/app/layout.tsx`
- `src/app/page.tsx`
- `src/app/SchemaOrg.tsx`
- `src/app/projects/page.tsx`
- `src/app/testimonials/page.tsx`
- `src/app/achievements/page.tsx`
- `src/app/case-studies/page.tsx`
- `src/app/case-studies/[slug]/page.tsx`
- `src/app/showcase/page.tsx`
- `src/app/showcase/[slug]/page.tsx`
- `src/components/Header.tsx`
- `src/components/Introduction.tsx`
- `src/components/MainNavigation.tsx`
- `src/components/Footer.tsx`
- `src/components/SEO.tsx` (OG site name / alt strings only)
- `public/manifest.json`

Optional if a title still contains Module Lead: `src/app/projects/[slug]/page.tsx`

## UI notes

- Layout: **no change**. Same sections, same buttons, same grid.
- Tokens / components: reuse existing classes. Do not restyle.
- Breakpoints: unchanged.
- Do not remove Earth, timeline, tech tabs, or expertise cards here.

## Acceptance criteria

- [x] `/` H1 is `Rajon Dey`. Role line is `Senior Software Engineer`. No Module Lead. No researcher title on the hero.
- [x] Hero supporting copy names full-stack work first and AI / LLM security research second.
- [x] Hero CV opens `https://www.rajondey.com/cv.pdf` and is labeled `CV`.
- [x] Hero GitHub is `https://github.com/RajonDey`.
- [x] Introduction is the two paragraphs above. No banned phrases.
- [x] Root `metadata` title and description match the identity table (years still computed).
- [x] JSON-LD `jobTitle` is `Senior Software Engineer`. Skills list matches the spec. No Three.js in schema skills.
- [x] `public/manifest.json` name/description no longer say "Software Developer Portfolio" as the primary identity.
- [x] Nav CV label is `CV` (not Download CV).
- [x] CTA bands on inner pages use Contact / email copy. No "passionate" / "innovative solutions" in those bands.
- [x] Experience timeline still shows Module Lead (Frontend) at SJ Innovation.
- [x] No new routes. No visual redesign.
- [ ] `npm run typecheck` passes — **blocked by pre-existing `/showcase` WIP imports**, not by this spec. Non-showcase files typecheck clean.
- [x] Browser-check `/` plus one inner page (e.g. `/projects`) for title, hero, CV, GitHub.

## Open questions

- Academic CV URL for scholarships: none in the career brief. Stay omitted until the owner provides one (`0.4` or `0.7`).
- Still deferred: home evidence items, WhatsApp vs email, relocation line, frontend CV placement.

UI/UX is **not** an open question for this spec. It is `0.8`, after `0.4`.

## Notes for the agent

- Status stays `draft` until the owner marks it `ready` or says to implement.
- If a file is WIP/broken (`/showcase` imports), still fix **copy strings** in those files if they contain banned phrases. Do not fix showcase imports in this spec.
- Prefer one shared title/description helper only if it is a small, obvious extract. Do not invent a copy CMS.
- `https://rajondey.com/cv.pdf` and `https://www.rajondey.com/cv.pdf` should keep working; do not switch the public button to a Google Doc.
- After implementation: set this spec to `done` and update `context/progress-tracker.md`.
