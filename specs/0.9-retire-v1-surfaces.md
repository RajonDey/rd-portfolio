# 0.9 Retire v1 surfaces

## Status

`done`

## Goal

The public site is only the v2 document: `/`, `/work`, `/work/[slug]`, `/writing`, `/about`. Tutorial chrome (Earth, timeline, tech icons, hero animation, archives) is gone. Old archive URLs permanently redirect. `tsc` and `next build` pass.

## Why

`context/portfolio-v2.md` cut those tropes. `0.4`–`0.8` already replaced home, work, writing, about, and the visual system. The leftover files still break typecheck (`/showcase`) and would ship if someone mounted them. Listed next in `context/progress-tracker.md`.

## In scope

- Delete unmounted v1 UI and the archive App Router trees listed below.
- 308 redirects in `next.config.ts` for old archive URLs.
- Drop WhatsApp (no quiet link). Contact stays email / GitHub / LinkedIn on home and About.
- Delete `public/planet/` (this spec authorizes it).
- Remove unused npm packages (Three, R3F, Drei, lottie, vertical-timeline, lucide-react, next-seo if unused).
- Strip dead CSS (timeline overrides, testimonial quote marks, gradient tag classes).
- Drop hash URLs (`#projects`, `#experience`, `#contact`) from `public/sitemap.xml`.
- Update `AGENTS.md` and `context/architecture.md` so they no longer point at retired files.

## Out of scope

- Identity copy, work inventory, article bodies (`0.1`–`0.7`).
- Visual tokens (`0.8`). Do not restyle remaining pages.
- Titles / OG / JSON-LD rewrite (`0.10`).
- Deleting `public/images/achievements/` binaries (protected). The gallery route goes; the files stay.
- Deleting `src/lib/portfolio.ts` or `experiences` in `src/lib/data.ts` (v2 still reads them).
- Adding quotes back onto About.
- Dark mode. New routes.

## User-facing behavior

1. Open `/`. Same document as `0.4`/`0.8`. No Earth, no timeline, no tech icons, no WhatsApp.
2. Nav is still Work · Writing · About · CV.
3. Visit `/projects` → ends on `/work` (308). Visit `/projects/{slug}` → `/work/{slug}` (308).
4. Visit `/testimonials` or `/achievements` → ends on `/` (308). No quote wall. No certificate gallery.
5. Visit `/showcase` → `/work` (308). Visit `/showcase/{slug}` → `/work/{slug}` (308).
6. `/case-studies` redirects from `0.2` stay.
7. `/work`, `/writing`, `/about` unchanged in copy.

## Content / copy

No new sentences. Do not add a WhatsApp href anywhere.

Redirects (permanent, `permanent: true` = 308 in Next):

| Source | Destination |
| ------ | ----------- |
| `/projects` | `/work` |
| `/projects/:slug` | `/work/:slug` |
| `/testimonials` | `/` |
| `/achievements` | `/` |
| `/showcase` | `/work` |
| `/showcase/:slug` | `/work/:slug` |

Keep existing `/case-studies` → `/work` and `/case-studies/:slug` → `/work/:slug`.

Sitemap: keep `/`, `/work`, `/writing`, `/about`. Remove `#projects`, `#experience`, `#contact`. Do not add retired paths.

## Files to delete

**Routes**

- `src/app/projects/page.tsx`
- `src/app/projects/[slug]/page.tsx`
- `src/app/testimonials/page.tsx`
- `src/app/achievements/page.tsx`
- `src/app/showcase/page.tsx`
- `src/app/showcase/[slug]/page.tsx`

**Components** (entire folders where listed)

- `src/components/EarthCanvas.tsx`
- `src/components/Contact.tsx`
- `src/components/Header.tsx`
- `src/components/DeveloperAnimation.tsx`
- `src/components/ExpertiseCard.tsx`
- `src/components/Introduction.tsx`
- `src/components/SidebarNav.tsx`
- `src/components/SEO.tsx`
- `src/components/Experience/`
- `src/components/TechStack/`
- `src/components/Testimonials/`
- `src/components/Achievements/`
- `src/components/Showcase/`
- `src/components/Projects/`
- `src/components/Work/ProjectDetailArticle.tsx`

**Lib** (only if nothing v2 imports)

- `src/lib/testimonials.ts`
- `src/lib/certificates.ts`
- `src/lib/avatarUtils.ts`
- `techStack` export in `src/lib/data.ts` (keep `experiences`)

**Assets**

- `public/planet/` (all Earth textures/models)

**Keep**

- `src/lib/portfolio.ts`, `selected-work.ts`, `work-article.ts`, `writing.ts`, `about.ts`, `site.ts`, `experience.ts`
- `src/components/MainNavigation.tsx`, `Footer.tsx`, `ContactCta.tsx`, `Work/WorkArticleView.tsx`
- `public/images/portfolios/`
- `public/images/achievements/` (binaries only; no route)

After deletes, grep for leftover imports. Uninstall packages that nothing imports: `@react-three/drei`, `@react-three/fiber`, `three`, `react-lottie`, `react-vertical-timeline-component`, `lucide-react`, `next-seo` (confirm unused). Keep `framer-motion` (mobile nav), `react-icons` (hamburger), `sharp`.

## UI notes

- No new layout. Remaining pages already match `0.8`.
- `globals.css`: delete `.vertical-timeline-element-icon` rules, `.testimonial-text` rules, `.blue-text-gradient` / `.pink-text-gradient` / `.green-text-gradient`. Keep base paper/ink/type.
- `next.config.ts` `images.domains`: drop `randomuser.me` and `ui-avatars.com` if only testimonials used them.
- Breakpoints: unchanged.
- Motion: none added. Keep nav Framer collapse.

## Acceptance criteria

- [x] `/projects`, `/testimonials`, `/achievements`, `/showcase` 308 as in the table. `/work` and `/about` still 200.
- [x] No Earth, WhatsApp, timeline, TechStack, Header animation, SidebarNav, or showcase tree in `src/`.
- [x] `public/planet/` gone. `public/images/achievements/` still on disk.
- [x] Home/nav copy unchanged. No WhatsApp link.
- [x] `npm run typecheck` passes with **no** showcase exception.
- [x] `npm run build` passes.
- [x] Browser-check `/`, `/work`, `/writing`, `/about`, and one old URL redirect.

## Open questions

None left for implementation. Decisions locked in this spec:

| Question | Decision |
| -------- | -------- |
| Archive URLs | **308** (not 404/410). Projects/showcase → `/work`. Testimonials/achievements → `/`. |
| WhatsApp | **Drop.** No quiet link. |
| Certificate images | **Keep** binaries. Remove the gallery route. |
| Planet assets | **Delete.** |

Change a row before marking `ready` if you disagree.

## Notes for the agent

- Owner said proceed. Implement against this spec.
- Do not delete `portfolio.ts` data. Do not invent a testimonials block on About.
- `public/planet/` is protected in `context/ai-workflow-rules.md` until this spec; this spec is the instruction to delete it. Do not delete `public/images/achievements/`.
- After implementation: status `done`, update `context/progress-tracker.md`, `context/architecture.md`, `AGENTS.md`.
