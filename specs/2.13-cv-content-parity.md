# 2.13 CV content parity

## Status

`done`

## Goal

Experience bullets in `src/lib/data.ts` are the locked source for `/about` and the desk ATS draft. They match the owner-ratified list in this spec. No invented wins, metrics, employers, or titles.

## Why

`2.12` locked the apply file as the Google Doc. The ATS draft still compiles from `data.ts`. The owner proceeded without pasting a new Doc dump, so the existing `data.ts` list is the approved baseline. `/about` already uses that array. See `context/progress-tracker.md`.

## In scope

- Lock the current two roles and bullets (below). No rewrite unless a later spec pastes Doc text.
- Keep freelance 2015–2018. Keep the ~30% load-time bullet (re-approved by proceed).
- Keep `rankBullets`. Do not drop the SJI role. Do not invent bullets.
- Overlay: one line that ATS draft facts come from `data.ts`.
- Patch progress tracker and spec README.

## Out of scope

- Scraping Google Docs
- Cloning the Google Doc layout
- Changing apply-file policy (`2.12`)
- Discovery sources, interview prep
- Changing About intro / stack / location
- Auto-apply
- Reopening `2.0`–`2.12`

## User-facing behavior

1. `/about` experience list is the locked `data.ts` bullets.
2. ATS draft uses the same bullets (order may follow the JD).
3. Real applications still use the Google Doc (`2.12`).
4. To change facts later: paste Doc bullets into a new spec, then edit `data.ts`.

## Content / copy

Owner proceeded without a new paste. Locked from current `src/lib/data.ts`:

### Role 1 (current)

- Title: Module Lead (Frontend)
- Company: SJ Innovation LLC
- Date: May 2019 - Present
- Bullets:
  1. Designed and delivered scalable web applications, owning frontend architecture while collaborating on end-to-end system design.
  2. Built and maintained production systems using React, Next.js, TypeScript, and modern frontend tooling.
  3. Worked with backend teams on API design, data contracts, authentication, and feature integration.
  4. Contributed to backend services and integrations using Node.js and Python where required.
  5. Worked on CI/CD pipelines using GitHub Actions to automate build, test, and deployment workflows.
  6. Led technical discovery, estimation, and architectural discussions across frontend and full-stack features.
  7. Mentored engineers through code reviews and sprint execution; participated in technical interviews.
  8. Improved application performance and reliability, achieving ~30% load-time reduction on production systems.

### Role 2 (kept)

- Title: Software Developer (Freelance / Contract)
- Company: Freelance
- Date: 2015 – 2018
- Bullets:
  1. Built and delivered client-facing web applications for international clients.
  2. Worked across frontend development, backend integrations, and production deployments.

## Files likely involved

- `src/lib/data.ts` (unchanged; already matches this list)
- `context/job-search-profile.md`
- `context/progress-tracker.md`
- `specs/README.md`

## UI notes

None. About and ATS draft already read this array.

## Acceptance criteria

- [x] Locked bullets in this spec match `src/lib/data.ts`
- [x] No new bullet, title, employer, or metric
- [x] SJI role first; freelance kept
- [x] Overlay notes ATS draft facts come from `data.ts`
- [x] `/about` shows these bullets
- [x] `npm run typecheck` and `npm run lint` pass
- [x] `/about` checked

## Open questions

None. Freelance kept. ~30% line kept. Paste Doc text in a later spec if the master Doc differs.

## Notes for the agent

- Owner said proceed without filling empty Role slots. Do not invent Doc wording. Lock existing `data.ts`.
- Do not scrape Google Docs.
- `career-brief.md` wins on conflict.
- Do not reopen `2.0`–`2.12`.
