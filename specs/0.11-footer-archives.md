# 0.11 Footer archives

## Status

`done`

## Goal

`/testimonials` and `/achievements` exist again as quiet document lists. They are linked from the footer, not the primary nav. IEEE stays on `/writing`, not as a gallery lead.

## Why

Owner asked to keep these URLs after `0.9` 308’d them to home. They are secondary proof (colleagues, POM, certificates), not hiring IA. Footer is the right place: off-nav, always reachable. Depends on `0.7`–`0.9`.

## In scope

- Restore `src/lib/testimonials.ts` and `src/lib/certificates.ts` from existing git content. Do not invent quotes or awards.
- New Server Component pages at `/testimonials` and `/achievements` matching v2 document layout (`0.8` tokens).
- Footer text links: Testimonials, Achievements.
- Remove the `0.9` 308s for those two paths only.
- Add the two URLs to `public/sitemap.xml`.
- Exclude certificate id `iccit-2025-paper-participation` from the achievements list (paper lives on `/writing`).

## Out of scope

- Putting Testimonials or Achievements in the primary nav.
- Restoring filters, star ratings, carousels, card grids, or `avatarUtils`.
- Restoring `/projects` or `/showcase`.
- Quotes on About (`0.7` still none on About).
- Visual restyle (`0.8` already done).
- Full SEO pass (`0.10`).

## User-facing behavior

1. Open `/`. Nav is still Work · Writing · About · CV. Footer has Testimonials and Achievements as text links.
2. Click Testimonials. URL `/testimonials`. H1 Testimonials. Existing quotes as a text list (quote, name, role, company). No filters.
3. Click Achievements. URL `/achievements`. H1 Achievements. Existing awards/certificates as a text list (title, issuer, date). Image/PDF is an optional text link, not a trophy wall. No ICCIT paper row.
4. `/projects` and `/showcase` still 308 to `/work`.

## Content / copy

| Slot | Copy |
| ---- | ---- |
| Testimonials H1 | `Testimonials` |
| Testimonials intro | `Notes from colleagues.` |
| Achievements H1 | `Achievements` |
| Achievements intro | `Awards and certificates.` |
| Footer labels | `Testimonials` → `/testimonials`, `Achievements` → `/achievements` |

Document titles: `Testimonials · Rajon Dey`, `Achievements · Rajon Dey`.

## Files likely involved

- `src/lib/testimonials.ts` (restore)
- `src/lib/certificates.ts` (restore)
- `src/types/index.ts` (`Testimonial` type)
- `src/app/testimonials/page.tsx`
- `src/app/achievements/page.tsx`
- `src/components/Footer.tsx`
- `next.config.ts`
- `public/sitemap.xml`

## UI notes

- Same padding as `/writing`. Body `max-w-3xl`. Hairline list. Serif H1. Paper background.
- Footer: a `flex flex-wrap gap-6` text-link row above the copyright line, centered. `hover:underline`.
- No ContactCta on these pages (secondary). Footer yes.
- Motion: none.

## Acceptance criteria

- [x] `/testimonials` and `/achievements` 200. Not in the nav. In the footer.
- [x] Quotes and certificates come from restored `src/lib` files. No invented rows.
- [x] ICCIT paper certificate is not on `/achievements`.
- [x] `/projects` still 308s to `/work`.
- [x] `npm run typecheck` passes.
- [x] Browser-check footer → both pages.

## Notes for the agent

Owner asked to keep these URLs after `0.9`. Implement against this spec. Do not restore the old gallery components.
