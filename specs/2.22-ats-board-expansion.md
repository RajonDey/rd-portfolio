# 2.22 ATS board expansion

## Status

`done`

## Goal

Find jobs watches additional owner-named Greenhouse / Ashby / Personio / Lever boards, using the existing `2.4` adapters. No invented employers. Techreviewer is not a source.

## Why

The eight starter boards in `2.4` were a legal DE/NL watchlist, not the market. Extra tokens were never named (`2.14`). Arbeitnow + `2.21` feeds are the broad net; company boards are “also watch these career pages.” This spec cannot go `ready` until the table below has owner-named rows.

## In scope

- Add rows to `src/lib/desk/sources.ts` **only** from the owner table in this spec. Each row: `provider`, `token` (ATS slug), `company`.
- Verify each feed URL with the existing `boardUrl` helper (no new adapters). Drop a row that 404s rather than inventing a different slug.
- Curated boards remain not IND-filtered (`2.14`).
- Overlay discovery sources: “owner-named ATS boards in `sources.ts`” plus the new names.
- Patch progress tracker, spec README.

## Out of scope

- Inventing companies from Techreviewer, Clutch, or similar vendor directories (those list agencies to hire, often US / Portugal / Estonia)
- Workday / SmartRecruiters / Recruitee adapters
- Scraping Greenhouse.io’s marketing site as a job board (Greenhouse is already the adapter)
- Relocate / LinkedIn / Wellfound / YC
- Changing fit, score, or queries
- Auto-apply, public site

## User-facing behavior

1. Find jobs includes the new boards in the same Inbox list (company name as today).
2. One board failing still does not fail the scan.
3. Public site unchanged.

## Content / copy

No new Desk strings unless a board’s `company` label is the display name.

### Owner-named boards (fill before `ready`)

Starter boards from `2.4` stay. Owner confirmed these extra rows on 2026-09-03:

| Provider | Token | Company |
| --- | --- | --- |
| ashby | `babbel` | Babbel |
| greenhouse | `d2l` | D2L |
| greenhouse | `storyblok` | Storyblok |
| ashby | `1password` | 1Password |
| ashby | `wealthsimple` | Wealthsimple |
| ashby | `cohere` | Cohere |
| greenhouse | `raisin` | Raisin |
| ashby | `miro` | Miro |

If this table is empty, status stays `draft`. Do not copy Techreviewer featured agencies.

## Files likely involved

- `src/lib/desk/sources.ts`
- `context/job-search-profile.md`
- `context/progress-tracker.md`
- `specs/README.md`

## UI notes

None. Same Inbox.

## Acceptance criteria

- [x] Every new row was named by the owner in this spec’s table
- [x] Find jobs fetches those boards with existing adapters
- [x] No invented tokens
- [x] Unauthenticated `/desk/discover` stays 404
- [x] `npm run typecheck` and `npm run lint` pass
- [x] Find jobs checked (or curl)

## Open questions

None. Owner confirmed the table.

## Notes for the agent

- Do not use Techreviewer rankings as a company list.
- Personio XML host stays `{token}.jobs.personio.de`.
- Keep the `2.1` gate. Never auto-submit.
