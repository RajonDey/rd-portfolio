# CLAUDE.md

This repository’s source of truth for agents is **`AGENTS.md`**.

On every task:

1. Read `AGENTS.md` (navigator: which context file and which source files to open).
2. Read `context/progress-tracker.md`.
3. For feature work, use a `ready` spec under `specs/` (see `specs/README.md`).
4. Follow `context/ai-workflow-rules.md`.

Do not skip the navigator and explore the whole repo by default.
